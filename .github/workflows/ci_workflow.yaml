name: Creación y subida de la imagen de angular
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    # Instalar docker
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: install Docker
        run: |
          sudo apt-get update
          sudo apt-get install ca-certificates curl
          sudo install -m 0755 -d /etc/apt/keyrings
          sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
          sudo chmod a+r /etc/apt/keyrings/docker.asc
          echo \
          "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
          $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
          sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
          sudo apt-get update
          sudo apt-get install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
          sudo docker run hello-world

# crear imagen de docker con el fichero docker file etiwuetandle extrayendo la vesion del docker file
      - name: Llamar a la action que me crea el nombre de la imagen
        uses: ./.github/actions/tagger_action
      #- name: Docker image creation
       # run: |
        #  sudo docker build -t javier/angular_app:0.0.0 .
         # sudo docker images
          

# subirla a docker hub usando las credenciales en los secrets
       #- name: Set up Docker for production
        #if: github.ref == 'refs/heads/main'
        #run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin


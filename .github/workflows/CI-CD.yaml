name: Construcción y Despliegue de App

on:
  workflow_dispatch:

jobs:
  call_workflow_CI: 
    uses: ./.github/workflows/ci_workflow.yaml
    secrets: inherit

  call_workflow_CD:  
    needs: call_workflow_CI
    uses: ./.github/workflows/cd_workflow.yaml
    secrets: inherit
    with:
      image_name: ${{ jobs.call_workflow_CI.outputs.image_name }}
